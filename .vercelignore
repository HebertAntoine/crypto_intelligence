# The root Vercel project only deploys the Flutter web app. Keep the Python
# backend, local data, virtualenvs, and docs out of the upload so Vercel never
# tries to build a serverless API from this repository root.
*
!app/
!app/**
!vercel.json
