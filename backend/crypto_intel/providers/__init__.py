"""Acquisition layer. The ONLY place allowed to touch the network, parse
external formats, or read import files. Everything above receives normalized
Observations and does not know where they came from."""
